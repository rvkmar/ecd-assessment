# version: "3.9"

services:
  ecd-assessment:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ecd-assessment
    restart: unless-stopped
    networks:
      - caddy_net

networks:
  caddy_net:
    external: true